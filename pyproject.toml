[project]
name = "projetos-dos-guri"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Felipe", email = "felipe_nascimento161@estudante.sesisenai.org.br" }
]
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
]

[project.scripts]
projetos-dos-guri = "projetos_dos_guri:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
